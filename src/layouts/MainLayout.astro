---
import MenuIcon from "../components/Icon/MenuIcon.astro";
import SunIcon from "../components/Icon/SunIcon.astro";
import i18next, { t } from "i18next";
import { LanguageSelector } from "astro-i18next/components";

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang={i18next.language}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-stone-900 text-white font-sans">
		<header
			class="fixed inset-x-0 top-[20px] flex flex-row justify-center m-auto text-sm"
		>
			<nav
				class="flex flex-row items-center backdrop-blur-md bg-black/30 rounded-full py-2 px-6 w-8/12 max-w-xs"
			>
				<ul class="w-full flex flex-row justify-between">
					<li
						class="text-transparent bg-clip-text bg-gradient-to-r from-green-300 via-blue-500 to-purple-600"
					>
						{t("home.nav.home")}
					</li>
					<li>{t("home.nav.about-me")}</li>
					<li>{t("home.nav.blog")}</li>
				</ul>
			</nav>
			<div
				class="w-10 h-10 backdrop-blur-md bg-black/30 flex flex-row items-center justify-center rounded-full"
			>
				<select
					id="language-selector"
					onchange="location = this.value;"
					class="appearance-none bg-black/0 w-4 h-4"
				>
					<option data-display="🇬🇧" value="/" selected>🇬🇧</option>
					<option data-display="🇪🇸" value="/es">🇪🇸</option>
				</select>
			</div>
		</header>

		<main class="mx-6">
			<slot />
		</main>
	</body>
	<style is:global>
		html {
			scroll-behavior: smooth;
		}
	</style>
</html>

<script>
	import { DEFAULT_LANG, languages } from "../shared/constants";

	const $ = (selector) => document.querySelector(selector);
	const languageSelector = $("#language-selector");
	const styleModeSelector = $("#style-mode-selector");

	const detectLanguage = () => {
		const { pathname } = window.location;
		const [, pathLanguageCode] = pathname.split("/");
		const language =
			languages[pathLanguageCode || DEFAULT_LANG] || DEFAULT_LANG;

		languageSelector.value = language?.slug;
	};

	document.addEventListener("DOMContentLoaded", function (event) {
		detectLanguage();
	});
</script>
