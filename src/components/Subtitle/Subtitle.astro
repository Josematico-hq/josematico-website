---
const { subtitle, centered, level } = Astro.props;
const TagName = level || "h2";
---

<TagName
    class={`text-4xl md:text-6xl font-bold dark:text-white ${
        centered && "text-center"
    }`}
>
    {subtitle}
</TagName>
